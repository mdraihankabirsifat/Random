/*
Problem: Find if Path Exists in Graph

Given an undirected graph and two vertices, determine whether any path
connects the source vertex to the destination vertex.
*/
#include <bits/stdc++.h>
using namespace std;

class Solution
{
public:
    bool validPath(int n, vector<vector<int>> &edges, int source, int destination)
    {
        for (int i = 0; i < edges.size(); i++)
        {
            
        }
    }
};
