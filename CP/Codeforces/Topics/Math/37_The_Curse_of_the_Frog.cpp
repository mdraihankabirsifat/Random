// https://codeforces.com/problemset/problem/2189/B
/*
Problem: The Curse of the Frog

Choose among the frog's permitted jump lengths to reach the target
position while minimizing backward jumps, or report impossibility.
*/

#include <bits/stdc++.h>
#define ll long long
#define loop(i, j, n) for (ll i = j; i < n; i++)
#define in(v) loop(i, 0, v.size()) cin >> v[i]
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll t;
    cin >> t;
    while (t--)
    {
        ll x, n, a, b, c;
        cin >> n >> x;
        vector<int> v(n);
        in(v);
    }
    return 0;
}
