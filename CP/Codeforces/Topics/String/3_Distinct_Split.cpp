// https://codeforces.com/problemset/problem/1791/D
/*
Problem: Distinct Split

Split a string into two nonempty parts and maximize the sum of the
numbers of distinct characters in the two parts.
*/

#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        string s;
        cin >> s;
        for (int i = 0; i < n; i++)
        {
        
        }
    }
    return 0;
}
