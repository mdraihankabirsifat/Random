// https://codeforces.com/problemset/problem/2091/B
/*
Problem: Team Training

Form teams from students with given skill levels so that each team's
size times its minimum skill is at least x; maximize the number of teams.
*/

#include <stdio.h>
int main()
{
    int t, n, x;
    scanf("%d", &t);
    for (int i = 0; i < t; i++)
    {
        scanf("%d %d", &n, &x);
        int arr[n];

    }

    return 0;
}
